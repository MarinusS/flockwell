# This file is sourced by Bash. Only use trusted shell content.
# Copy to .env and replace the path with your service account JSON file.
FLOCKWELL_GOOGLE_CREDENTIALS_JSON="$(cat /path/to/google-credentials.json)"
